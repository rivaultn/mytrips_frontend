/*
 *
 * AddingModalTripEditingContainer reducer
 *
 */

import { fromJS } from 'immutable';

export const initialState = fromJS({});

function addingModalTripEditingContainerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default addingModalTripEditingContainerReducer;
