// import { fromJS } from 'immutable';
// import { selectAddingModalComponentDomain } from '../selectors';

describe('selectAddingModalComponentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
