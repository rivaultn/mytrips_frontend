/*
 *
 * AddingModalStepEditingContainer reducer
 *
 */

import { fromJS } from 'immutable';

export const initialState = fromJS({});

function addingModalStepEditingContainerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default addingModalStepEditingContainerReducer;
