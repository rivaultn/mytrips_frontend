// import { fromJS } from 'immutable';
// import { selectAddingModalStepEditingContainerDomain } from '../selectors';

describe('selectAddingModalStepEditingContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
