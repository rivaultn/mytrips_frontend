// import { fromJS } from 'immutable';
// import { selectAddingModalPhotoEditingContainerDomain } from '../selectors';

describe('selectAddingModalPhotoEditingContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
