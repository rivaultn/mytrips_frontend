/*
 *
 * SubstepEditingContainer reducer
 *
 */

import { fromJS } from 'immutable';

export const initialState = fromJS({});

function substepEditingContainerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default substepEditingContainerReducer;
