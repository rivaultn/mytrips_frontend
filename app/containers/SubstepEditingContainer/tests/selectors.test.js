// import { fromJS } from 'immutable';
// import { selectSubstepEditingContainerDomain } from '../selectors';

describe('selectSubstepEditingContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
