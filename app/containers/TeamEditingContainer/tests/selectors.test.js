// import { fromJS } from 'immutable';
// import { selectTeamEditingContainerDomain } from '../selectors';

describe('selectTeamEditingContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
